export const addItem = () => {
     // const updatedWorkerNames = [...day.shifts[indexD].workerNames, name];
    // const updatedShift = {...day.shifts[indexD],workerNames: updatedWorkerNames};
    // const updatedDay = {...day,shifts: [...day.shifts.slice(0, indexD),updatedShift,...day.shifts.slice(indexD + 1)]};
    // const updateAll = [...schedule.slice(0, indexKey),updatedDay,...schedule.slice(indexKey + 1)];
    // console.log(updateAll);
    //handleShowModal(null);
};

export const deleteItem = () => {

    // console.log(indexKey,index,i);
    // const add = [...schedule[indexKey].shift[indexD].workerNames, schedule[indexKey].shift[indexD].workerNames.filter((item, key) => key !== i)]
    // console.log("delete "+add);
    // const updatedWorkerNames = day.shifts[index].workerNames.filter((item, key) => key !== i);
    // const updatedShift = {...day.shifts[index],workerNames: updatedWorkerNames};
    // const updatedDay = {...day,shifts: [...day.shifts.slice(0, index),updatedShift,...day.shifts.slice(index + 1)]};
    // const updatedSchedule = [...schedule.slice(0, indexKey),updatedDay,...schedule.slice(indexKey + 1)];
    // console.log(updatedSchedule);
    // dispatch(сompanySchedule(updatedSchedule));
};
